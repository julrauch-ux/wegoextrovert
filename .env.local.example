# Vercel KV — copy these from your Vercel project dashboard
# (Storage → KV → Connect → .env.local)
# Without these, the API falls back to local data/waitlist.json

KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
